_schema-version: 3.3.0
ID: Claims_Project
description: A simple CAP project.
version: 1.0.0
modules:
- name: Claims_Project-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Claims_Project-auth
  - name: Claims_Project-db
  - name: Claims_Project-connectivity
  - name: Claims_Project-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Claims_Project-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Claims_Project-db
  parameters:
    buildpack: nodejs_buildpack
- name: Claims_Project
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Claims_Project-auth
  - name: Claims_Project-destination
  - name: Claims_Project-html5-runtime
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: Claims_Project-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: Claims_Project-auth
  - name: srv-api
  - name: Claims_Project-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - claimsprojectclaimsproject.zip
      name: claimsprojectclaimsproject
      target-path: app/
- name: Claims_Project_DB_Module
  type: hdb
  path: Claims_Project_DB_Module
  requires:
  - name: hdi_Claims_Project_DB_Module
- name: claimsprojectclaimsproject
  type: html5
  path: app/claims_project
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Claims_Project-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: Claims_Project-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: Claims_Project-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Claims_Project-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-plan: lite
- name: Claims_Project-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: Claims_Project-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: Claims_Project-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: hdi_Claims_Project_DB_Module
  type: com.sap.xs.hdi-container
  properties:
    hdi-container-name: ${service-name}
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
